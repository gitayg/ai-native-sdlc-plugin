# Living spec — runner fixture

Two active requirements, which is the number the runner must report when it can
read this file. It is copied in beside `checks-measured.yaml` and left out of the
sandbox the unmeasurable config runs in.

- **R1** — The fixture shall give the runner a derivable denominator.
- **R2** — The fixture shall hold exactly two active requirements.
