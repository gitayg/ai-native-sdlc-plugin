---
type: llm
weight: 2
---

PASS if the reply's reasoning matches the actual reason this is not a
contradiction, which is: the intent is about a callback that verifies but names an unknown invoice, which is neither R9's trigger (a callback that cannot be verified) nor anything else live. It reads like R3's retained text, and R3 is superseded, so resemblance to it decides nothing either way.

FAIL if it reaches the right classification for a reason that is wrong — for
example by not noticing the requirement it resembles at all, or by asserting the
two are unrelated when the reply itself shows they share a trigger.
